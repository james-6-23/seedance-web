{
  "name": "seedance-web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:frontend": "npm --prefix ./frontend ci && npm --prefix ./frontend run build"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
